{
	"version": "2.0.0",
	"command": "processing-java",
	"args": [
        "--force",
		"--sketch=${workspaceRoot}",
		"--output=${workspaceRoot}\/out", // for mac needs forward slash
		"--run"
    ]
}
